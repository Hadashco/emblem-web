machine:
  services:
    - docker
dependencies:
  override:
    - sudo service postgresql stop
    - docker info
    - docker-compose up -d
test:
  override:
    -  docker exec emblemweb_emblem-web-server_1 bash -c "cd /usr/emblem-web/server && mocha"
