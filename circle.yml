machine:
  services:
    - docker
  node:
    version: 6.3.0
dependencies:
  override:
    - sudo service postgresql stop
    - docker info
    - docker-compose up -d
    - cd server && npm install mocha -g && npm install
test:
  override:
    -  npm run test
