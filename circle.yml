machine:
  services:
    - docker
  node:
    version: 6.3.0
dependencies:
  override:
    - sudo service postgresql stop
    - docker info
    - docker-compose up -d
test:
  override:
    - cd server && sudo npm install mocha -g && sudo npm install && sudo npm run test
